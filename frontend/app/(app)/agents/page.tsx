import { Placeholder } from "@/components/placeholder";

export default function AgentsPage() {
  return (
    <Placeholder
      title="Agents"
      hint="Build workflows on a canvas and run them against your models and notebooks."
      milestone="M5 (engine) and M6 (canvas)"
    />
  );
}
